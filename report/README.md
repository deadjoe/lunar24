<!-- SPDX-License-Identifier: Apache-2.0 -->
# report/ — 阶段性分析报告

本目录存放对 Lunar 24 的**阶段性分析与审计报告**。

## 与其他目录的分工

| 目录 | 内容 | 谁写 |
|---|---|---|
| `design/` | 设计规范与计划（**是什么、要做成什么样**） | 设计线 |
| `design/00-status.md` | 滚动的实施状态与裁决流水账 | 工程总监 |
| `FINDINGS.md` | 测量记录与 provisional 常数台账 | 实施线 |
| `review/`（分支 `review/codex-*`） | 逐批次代码审查记录 | 审查线 |
| **`report/`** | **某一时点的整体分析/审计快照**（**现在处在哪、有什么问题**） | 外部或阶段性审计 |

`report/` 与 `review/` 的区别：`review/` 是**逐批次、面向单个改动**的审查；`report/` 是**跨阶段、面向整个项目状态**的快照，含独立复测与优先级建议。

## 命名约定

```
report/YYYY-MM-DD-<slug>.md
```

## 每份报告必须包含

1. **审计基线**：exact head commit、分支、工作树状态、当次门禁/CI 结果
2. **证据口径**：区分 `[实测]` / `[代码]` / `[手册]` / `[推断]`
3. **未验证范围的显式声明** —— 防止后续被当成已验证事实引用
4. **可复现方法**：探针源码或命令，且**必须实际验证过可原样运行**
5. **发现项与既有 GitHub issue 的对应关系**（哪些是新的、哪些已被跟踪）

## 快照规则与勘误

报告一旦提交即为**该时点的快照**。**结论、优先级与判断不得事后美化重写**——情况变化时另出新报告，不改旧文件。

**唯一例外是勘误（ERRATA）**：当报告含有**事实错误或会把错误知识传给实施者的方法学错误**时，就地更正，但必须：

1. 在文件顶部加 `## ERRATA` 小节，逐条列出**改了什么 / 首版原文 / 更正内容 / 依据**；
2. 在被改动的正文处留下 `[EN 已修订 YYYY-MM-DD]` 标记，指回 ERRATA 条目；
3. 明确列出**未更改的部分**，防止读者以为整份被重做；
4. 以**独立 commit** 提交，commit message 说明勘误来源。

理由：快照规则的目的是防止事后粉饰，不是要求继续传播已知错误。

## 索引

| 日期 | 报告 | 基线 | 摘要 |
|---|---|---|---|
| 2026-09-06 | [项目整体独立审计](2026-09-06-project-audit.md) | `4a4c7b9` | 设计方向正确、工程纪律优异；实施尚未构成可演奏乐器。8 项新发现，其中 3 项 🔴（DRY B 开机直流钉死 / 全链路无带限 / VCF `sr/8` 跨采样率不一致）。**含 ERRATA E1–E7（2026-09-06）** |
